// src/application/ports/IExternalService.js

const IExternalService = {
  /**
   * Simulate a quantum coherence model (e.g., for decision weights).
   * @param {Object} params - Simulation parameters (e.g., coherence, energy).
   * @returns {Promise<number>} - Resonance value.
   */
  simulateCoherence: async (params) => {},
};

export default IExternalService;