// src/domain/services/PrerequisiteValidator.js

export class PrerequisiteValidator {
    static validatePrerequisites(interaction, playerState) { /* as in doc */ }
    static validatePrerequisiteGroup(group, playerState) { /* as in doc */ }
    static validateCondition(condition, playerState) { /* as in doc */ }
    // ... all static validation methods
  }